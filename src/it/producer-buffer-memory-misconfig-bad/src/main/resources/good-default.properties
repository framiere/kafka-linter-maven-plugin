# Control for PRODUCER_BUFFER_MEMORY_MISCONFIG — none of the three sub-cases fires.
#
# buffer.memory = 33554432 (32 MiB, the kafka-clients default).
# batch.size    = 16384    (16 KiB, the default).
# Check sequence: buffer (32 MiB) < batch (16 KiB)? No. buffer < 1 MiB? No.
# buffer > 1 GiB? No. Rule emits zero violations.

buffer.memory=33554432
batch.size=16384
