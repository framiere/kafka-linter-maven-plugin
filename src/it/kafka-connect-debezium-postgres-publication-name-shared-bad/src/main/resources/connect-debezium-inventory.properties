name=inventory-cdc
connector.class=io.debezium.connector.postgresql.PostgresConnector
tasks.max=1

database.hostname=inventory-pg.internal
database.port=5432
database.user=debezium
database.password=changeme
database.dbname=inventory

topic.prefix=cdc.inventory

slot.name=debezium_inventory
plugin.name=pgoutput

# Anti-pattern: shares publication.name=dbz_shared_pub with the orders
# connector in this project (see the long explanation in
# connect-debezium-orders.properties). Last-writer-wins on the publication
# table-list silently breaks one of the two connectors on every startup.
publication.name=dbz_shared_pub
publication.autocreate.mode=filtered

table.include.list=public.products,public.stock_movements

key.converter=io.confluent.connect.avro.AvroConverter
key.converter.schema.registry.url=http://schema-registry:8081
key.converter.auto.register.schemas=false
value.converter=io.confluent.connect.avro.AvroConverter
value.converter.schema.registry.url=http://schema-registry:8081
value.converter.auto.register.schemas=false

snapshot.mode=initial
