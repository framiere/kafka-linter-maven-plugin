name=gcs-analytics-sink
connector.class=io.confluent.connect.gcs.GcsSinkConnector
tasks.max=4

topics=events.clicks,events.impressions

gcs.bucket.name=analytics-data-lake
gcs.part.size=5242880
gcs.credentials.path=/etc/secrets/gcs-service-account.json

# Anti-pattern: very large flush.size with no time-based rotation.
# At realistic throughput (5K records/sec/partition × 2KB/record) the
# per-partition heap buffer grows by 10MB/sec — fills 20GB in ~33 min,
# OOMing the Connect worker long before flush.size fires. Additionally,
# every worker crash mid-upload leaves an abandoned resumable-upload
# session that counts against the GCS project's API quota for ~24h.
flush.size=10000000

format.class=io.confluent.connect.gcs.format.parquet.ParquetFormat
partitioner.class=io.confluent.connect.storage.partitioner.TimeBasedPartitioner
partition.duration.ms=3600000
path.format='year'=YYYY/'month'=MM/'day'=dd/'hour'=HH
locale=en-US
timezone=UTC
timestamp.extractor=Record

key.converter=org.apache.kafka.connect.storage.StringConverter
value.converter=io.confluent.connect.avro.AvroConverter
value.converter.schema.registry.url=http://schema-registry:8081
value.converter.auto.register.schemas=false
