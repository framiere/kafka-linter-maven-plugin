name=debezium-inventory-cdc
connector.class=io.debezium.connector.postgresql.PostgresConnector
tasks.max=1

database.hostname=inventory-db.internal
database.port=5432
database.user=debezium
database.password=secret
database.dbname=inventory
topic.prefix=cdc.inventory
plugin.name=pgoutput

# Anti-pattern: shared slot.name (see connect-debezium-orders.properties).
slot.name=debezium_shared

table.include.list=public.products,public.stock_levels

schema.history.internal.kafka.topic=cdc.inventory.schema-history
schema.history.internal.kafka.bootstrap.servers=kafka-1.prod.example.com:9092

key.converter=io.confluent.connect.avro.AvroConverter
key.converter.schema.registry.url=http://schema-registry:8081
key.converter.auto.register.schemas=false
value.converter=io.confluent.connect.avro.AvroConverter
value.converter.schema.registry.url=http://schema-registry:8081
value.converter.auto.register.schemas=false
