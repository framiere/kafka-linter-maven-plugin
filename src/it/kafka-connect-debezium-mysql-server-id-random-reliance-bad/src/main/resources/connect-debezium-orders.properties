name=debezium-orders-cdc
connector.class=io.debezium.connector.mysql.MySqlConnector
tasks.max=1

database.hostname=orders-db.internal
database.port=3306
database.user=debezium
database.password=secret
database.include.list=orders
topic.prefix=cdc.orders

# Anti-pattern: database.server.id is not declared. Debezium will pick a
# random integer in [5400, 6400] at every task startup. The 1000-wide
# window has a ~50% birthday-collision probability at 37 connectors,
# and the per-restart re-randomization makes MySQL audit-log queries
# and DBA observability impossible.
# Fix: database.server.id=<int> from the org's server-id allocation registry.

table.include.list=orders.orders,orders.order_items

schema.history.internal.kafka.topic=cdc.orders.schema-history
schema.history.internal.kafka.bootstrap.servers=kafka-1.prod.example.com:9092

key.converter=io.confluent.connect.avro.AvroConverter
key.converter.schema.registry.url=http://schema-registry:8081
key.converter.auto.register.schemas=false
value.converter=io.confluent.connect.avro.AvroConverter
value.converter.schema.registry.url=http://schema-registry:8081
value.converter.auto.register.schemas=false
